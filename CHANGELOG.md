# 更新日志

## v1.7.0

### 新功能

- B站视频现在会先发一张海报再发视频：封面、标题、UP 主、发布时间、播放 / 弹幕 / 点赞 / 投币 / 收藏 / 转发 / 评论、简介、“热门收录”这类标签都在上面，登录后还带 AI 总结。海报是单独一条消息，视频跟在后面，不会被藏进合并转发里。不想要可以在 B站解析器里关掉 `渲染视频海报`。
- B站评论区图换成了和 B站网页一个样子的：头像、大会员粉色昵称、等级徽章、UP / 置顶标记、“UP主觉得很赞”、发布时间、IP 属地、点赞数、配图，`[doge]` 这种 B站表情也会显示成图片。每条热评下面带楼中楼（和网页一样是前几条回复），后面写着“共 N 条回复”。`评论区显示楼中楼` 可以关。
- 海报和评论区图都是网页模板渲染的。新增全局配置 `HTML 渲染引擎`：默认自动，优先本地 Playwright（第一次用会自己下载 Chromium，约 150 MB），没浏览器就退回 AstrBot 自带的网络文转图，不用装任何东西也能出图。
- B站的 QQ 小程序分享卡片、PC 端分享卡片都能触发解析了，卡片前面带 @ 或者文字也认得。LLM 工具模式下 LLM 看不见卡片，所以卡片会直接唤醒解析，媒体直接发到当前会话。

### 修复

- 多 P 视频的分 P 没有首帧图时，海报和卡片不再没封面，改用视频封面。
- 置顶和 UP 主的评论以前只要带 @ 就会被排到后面，现在按原来的顺序放在前面。

## v1.6.0

### 新功能

- 抖音可以选清晰度了（1080P / 720P / 540P）。选的档位不存在或者文件太大超过体积限制，会自动降一档，不再是“哪个文件大就发哪个”。
- 官 Bot 模式多了两个开关：`视频体积上限`（默认 30 MB，腾讯超过这个数就不当视频、改当文件卡片发）和 `强制最低清晰度`（默认开）。AstrBot 太旧、不支持大文件分片上传的话，插件会自己按 10 MB 来。
- 官 Bot 模式和 LLM 工具模式下，图集超过 4 张（可调）会拼成一张长图发，不再一张张发到撞上 QQ 的回复次数上限。
- 小黑盒游戏页换了新的取数据方式，PC / 主机 / 手游都能解析了。

### 修复

- “此项媒体下载失败”终于会说原因了（比如 HTTP 403、超时），日志里也有同样一行；视频太长、解析失败也会告诉你为什么。
- 抖音下载不再随机挑线路，一条挂了自动换下一条；撞上抖音的人机验证会自动换一个分享域名再试；被删掉的视频会直接说“视频不可用”。
- 抖音视频时长以前算错了 1000 倍，卡片上会显示成几十万秒。
- 下载到一半的文件不会再被当成完整文件发出去；几个群同时解析同一个视频只会下载一次。
- 小黑盒帖子被要求人机验证时，会提示去填 Cookie，而不是莫名其妙失败。
- 一些小修：Cookie 过期时间解析、yt-dlp 报错不再让整个解析崩掉。

## v1.5.5

### 修复

- 修复扫码登录保存的 B站凭证未用于评论接口，导致评论区始终以访客态请求、通常只能取得 3 条评论的问题。
- 评论抓取增加置顶评论、WBI 接口和旧分页接口兜底，并在接口限制评论数量时输出明确日志。

### 新增

- 新增 LLM 工具模式；开启后停止普通消息主动解析，不再贴表情或发送开始解析提示。
- 新增 `parse_media_link` LLM 工具，媒体直接发送到当前会话，并向 LLM 返回结构化成功或失败状态。

## v1.5.4

### 上游同步

- 同步原版至 `78ad291`：修复抖音链接匹配范围过宽的问题。
- 同步 Iwara、Twitter/X、小黑盒及其他解析器的最新兼容性调整。
- 同步解析器注册、配置模板、依赖版本与本地媒体路径处理更新。

### Fork 功能保留

- 保留 QQ 官 Bot 适配模式及官方富媒体发送逻辑。
- 保留 B站评论区抓取、过滤、渲染及与主视频合并配置。
- 保留 B站视频流异常时的 DASH/durl 手动挑流兜底。

## v1.5.3

变更:

- 合并原版最新解析器更新：新增 Iwara 图片/视频解析、微信视频号解析，并同步抖音请求容错改进。
- B站下载兼容 `hvc1` 新编码；官方流选择器异常、空流或无音频时安全回退到原始 DASH/durl 挑流。
- 修复评论区接口被 B站风控后静默消失：改用 `curl_cffi` 浏览器指纹会话预热并抓取，失败时输出明确日志。
- 官 Bot 适配模式改为配置开启即全局生效，彻底禁止构造合并转发节点，图片使用官方适配器可读取的本地文件组件。

## v1.5.2

### 新增

- 新增官 Bot 适配模式：开启后跳过 QQ 贴表情仲裁，命中链接时发送开始解析提示。
- 官 Bot 适配模式下不再使用 OneBot/NapCat 合并转发节点，媒体发送交由 QQ 官方富媒体接口处理。

- 增加 iwara 视频 / 图片解析器 @sssysy
- 新增微信视频号(shipinhao)解析器 @anon0v0

### 优化

- 扩展 Twitter 解析器，使其能够同时处理 twitter.com 和 x.com URL，并规范化处理 @Zhalslar

- 基于 SSR 为抖音链接新增规范分享页解析能力 @piexian

### 修复

- 修复本地媒体发送时的文件路径问题 @piexian
- 无匹配视频流时优雅报错，避免 IndexError 中断解析 @anon0v0

## v1.5.1

变更:

- 允许为 B 站下载指定一个“首选视频编码格式列表”，而不再局限于单一编码格式。

修复:

- 当基于凭证（如 cookies）的请求失败时，回退为警告信息，从而避免在获取 B 站 AI 总结时发生崩溃。
- 通过正确读取使用 `rich` 或 `orig_text` 字段而非 `word` 字段的 `TEXT_NODE_TYPE_RICH` 节点中的文本，在解析 Bilibili 动态时保留标签。

## v1.5.0

## 概述

新增知乎链接解析能力，支持对知乎内容进行识别、请求、内容提取与卡片生成，并以模块化结构实现，便于后续维护和扩展。

## 变更内容

- 新增知乎解析器 [ZhihuParser](cci:2://file:///c:/mine/codes/astrbot_plugin_parser/core/parsers/zhihu/parser.py:14:0-84:9)
- 支持知乎常见页面类型解析
- 新增知乎请求处理逻辑
- 新增知乎内容提取逻辑
- 新增知乎卡片生成逻辑
- 将知乎解析实现拆分为多个职责清晰的模块，降低耦合度

## 支持范围

当前已支持以下知乎内容解析：

- 文章
- 回答
- 问题页面
- 想法

## 实现说明

本次实现采用 mixin 组合方式组织知乎解析器，主要拆分如下：

- [parser.py](cci:7://file:///c:/mine/codes/astrbot_plugin_parser/core/parsers/zhihu/parser.py:0:0-0:0)
  - 解析器主入口
- `request.py`
  - 请求相关逻辑
- `handlers.py`
  - 不同页面/数据类型的处理逻辑
- `content.py`
  - 内容提取与格式化
- `card.py`
  - 卡片摘要与展示内容生成
- `common.py`
  - 公共工具与共享逻辑

这样的拆分方式可以让不同职责独立维护，也方便后续继续补充更多知乎页面类型或优化展示效果。

## 变更动机

知乎链接在日常使用中较为常见，但此前缺少对应解析支持。该 PR 旨在补齐知乎平台解析能力，提升插件对主流内容平台的覆盖范围和使用体验。

## 测试情况

已完成以下方向的功能验证：

- 知乎链接识别是否正确
- 不同知乎页面类型是否能正确路由到对应解析逻辑
- 文章 / 回答 / 问题 / 想法内容是否能正常提取
- 卡片摘要与发送头部信息是否符合预期

## 兼容性与风险

- 本次改动为新增平台解析能力，对现有其他平台解析逻辑影响较小
- 主要风险集中在知乎页面结构或接口返回字段变化时的兼容性
- 后续如知乎页面结构调整，预计只需在对应模块中局部修正

## v1.4.0

### 新增

- 新增小黑盒平台支持，并同步更新 README 支持列表。
- 新增 `core/parsers/xiaoheihe.py`，支持：
  - 小黑盒帖子链接与分享卡片解析
  - 小黑盒游戏详情页链接与分享卡片解析
- 新增标准内容类型 `TextContent`，用于承载独立文本消息项。
- 新增 `create_video_content_by_task(...)`，支持基于自定义下载任务构造视频内容。
- 新增 `ytdlp_download_video_relaxed(...)`，用于宽松处理特定视频下载场景。
- 新增小黑盒配置项：
  - `show_body_text`
  - `video_send_mode`

### 变更

- 扩展配置结构，正式纳入小黑盒解析器配置节点。
- 扩展 sender，对 `TextContent` 提供标准发送支持。
- 调整小黑盒视频发送策略：视频内容从合并转发中分离，改为单独发送。
- 调整小黑盒视频输出策略，支持以下模式：
  - `不发送视频`
  - `发送第一条视频`
  - `发送全部视频`
- 调整小黑盒 `.m3u8` 视频处理路径，改为使用宽松 yt-dlp 下载逻辑。
- 增强小黑盒游戏页解析结果，纳入图片、结构化文本与视频提取能力。

### 涉及文件

- `README.md`
- `_conf_schema.json`
- `core/config.py`
- `core/data.py`
- `core/download.py`
- `core/parsers/__init__.py`
- `core/parsers/base.py`
- `core/parsers/xiaoheihe.py`
- `core/sender.py`
- `default_template.json`
- `metadata.yaml`

### 说明

本次升级（包括小黑盒网页逆向）由 GPT 5.4 驱动的 AI 智能体协助完成。
本次升级已通过人工验收，不会影响任何插件已有的功能。
